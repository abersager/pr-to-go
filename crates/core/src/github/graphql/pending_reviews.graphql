query PendingReviews($prId: ID!) {
  node(id: $prId) {
    ... on PullRequest {
      reviews(states: [PENDING], first: 5) {
        nodes {
          id
          author {
            login
          }
        }
      }
    }
  }
}
