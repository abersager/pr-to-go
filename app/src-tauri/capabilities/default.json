{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window may call PR to Go commands and open links in the system browser.",
  "windows": ["main"],
  "permissions": ["core:event:default", "opener:allow-open-url"]
}
